{% extends "base.html.twig" %}

{% block title %}SymRecipe - Accueil
{% endblock %}

{% block body %}

	{# <div class="container mt-4"> #}
	<div class="jumbotron mt-4">
		<div class="border p-4">
			<div class="card text-center">
				<div class="card-header">
					<h1>Bienvenue sur SymRecipe</h1>
				</div>
				<div class="card-body">
					<h3 class="card-title">SymRecipe est une application qui va te permettre de créer des recettes à base d'ingrédients que tu auras toi-même créés.</h3>
					<hr class="my-4">
					<p class="card-text">
						<h5>Tu pourras partager tes recettes avec la communauté du site ou les garder en privé.</h5>
					</p>
					<a href="{{ path('recipe_recipe_community')}}" class="btn btn-primary">Aller c'est parti !</a>
				</div>
			</div>

			<div class="recipe mt-4">
				<h2 text align="center">Les recettes récentes de la communauté</h2>
				<div class="row justify-content-center">
					{% for recipe in recipe %}
						<div class="col-md-4 mb-4">
							<div class="card border-primary" style="width: 18rem;">
								<img src="{{ vich_uploader_asset(recipe, 'imageFile') }}" class="card-img-top" alt="{{ recipe.name }} image de recette">
								<div class="card-header bg-primary text-white">
									Recette n°{{ recipe.id }}
								</div>
								<div class="card-body">
									<h4 class="card-title">
										<a href="{{ path('recipe_show', {id: recipe.id}) }}">
											{{ recipe.name }}
										</a>
									</h4>
									<p class="card-text">{{ recipe.description |slice(0, 100) ~ '...' }}</p>
								</div>
							</div>
						</div>
					{% endfor %}
				</div>
			</div>

		</div>
	</div>

{% endblock %}
